export interface Grocery {
    id: string;
    name: string;
    quantity: number;
    quantityShopping: number;
    units: string;
    teamId: string;
    inShoppingList: boolean;
    picked: boolean;
}
